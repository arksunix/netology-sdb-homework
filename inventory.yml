all:
  hosts:
  children:
    master:
      hosts:
        vm1: #hostname
          ansible_host: 192.168.0.2 #ip
          ansible_user: vagrant
          ansible_ssh_private_key_file: /path/to/private_key
    mirror:
      hosts:
        vm2: #hostname
          ansible_host: 192.168.0.3 #ip
          ansible_user: vagrant
          ansible_ssh_private_key_file: /path/to/private_key
